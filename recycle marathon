import pgzrun
import random

WIDTH = 1000
HEIGHT = 750
TITLE = "rycling marathon"
non_r=["battery","bottle","chips","plastic bag"]
actors = []
levels = 5
cur_level = 1

def draw():
    screen.blit("eco backround",(0,0))
    for actor in actors:
        actor.draw()
def create_actors():
    global actors
    items = ["paper"]
    for i in range(cur_level):
        item = random.choice(non_r)
        items.append(item)
    for item in items:
        actor = Actor(item) 
        actors.append(actor)
        
def update():
    pass 
create_actors()

pgzrun.go()
